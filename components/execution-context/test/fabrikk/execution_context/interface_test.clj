(ns fabrikk.execution-context.interface-test
  (:require [clojure.test :as test :refer :all]
            [fabrikk.execution-context.interface :as execution-context]))

(deftest dummy-test
  (is (= 1 1)))
